# Frontend dev server port.
VITE_DEV_PORT=3000

# Backend server port. Keep this aligned with backend/config.yaml server.port
# or the SERVER_PORT used when starting the backend.
SERVER_PORT=8080

# Optional: override the full backend proxy target.
# VITE_DEV_PROXY_TARGET=http://localhost:8080
